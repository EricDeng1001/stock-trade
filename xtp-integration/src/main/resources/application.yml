xtp:
  server-ip: 1.1.1.1
  server-port: 80
  log-folder: /var/log
  client-id: 3
  username: xyz
spring:
  datasource:
    url: jdbc:h2:mem:jpadb
    driverClassName: org.h2.Driver
    username: sa
    password:
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    open-in-view: false
    hibernate:
      ddl-auto: create
  h2:
    console:
      enabled: true